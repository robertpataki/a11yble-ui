import { Props } from './TabbedInterface.type';

export const TabbedInterface = (props: Props) => {
  return (
    <div {...props}>
      <h1>Tabbed Interface</h1>
      <p>Let's create some tabbed interfaces</p>
    </div>
  );
};

export default TabbedInterface;
